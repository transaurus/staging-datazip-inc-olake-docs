---
title: "Docker Sync MongoDB Command - OLake Docker"
description: "Execute OLake sync for MongoDB using Docker. Replicate MongoDB collections to data lakes with containerized deployment."
---

<Tabs groupId="docker">

<TabItem value="macos-linux" label="macOS / Linux" default>

```bash
docker run --pull=always  \
  -v "$HOME/PATH_TO_OLAKE_DIRECTORY:/mnt/config" \
  olakego/source-mongodb:latest \
  sync \
  --config /mnt/config/source.json \
  --catalog /mnt/config/streams.json \
  --destination /mnt/config/destination.json
```
</TabItem>

<TabItem value="cmd" label="CMD">

```bash
docker run --pull=always  ^
  -v "%USERPROFILE%\PATH_TO_OLAKE_DIRECTORY:/mnt/config" ^
  olakego/source-mongodb:latest ^
  sync ^
  --config /mnt/config/source.json ^
  --catalog /mnt/config/streams.json ^
  --destination /mnt/config/destination.json
```
</TabItem>

<TabItem value="powershell" label="Powershell">

```bash
docker run --pull=always  `
  -v "$env:USERPROFILE\PATH_TO_OLAKE_DIRECTORY:/mnt/config" `
  olakego/source-mongodb:latest `
  sync `
  --config /mnt/config/source.json `
  --catalog /mnt/config/streams.json `
  --destination /mnt/config/destination.json
```

</TabItem>
</Tabs>